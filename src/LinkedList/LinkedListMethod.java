package LinkedList;

public interface LinkedListMethod {
	
	/**
	 * 在末尾添加一个节点
	 * @param d
	 */
	public void addNode(Node head,int d);
	
	/**
	 * 返回当前链表长度
	 * @return
	 */
	public int length(Node head);
}
